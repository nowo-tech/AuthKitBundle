name: auth-kit-bundle-demo-symfony-8

services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: auth-kit-demo-symfony8-php
    working_dir: /app
    ports:
      - "${PORT:-8010}:80"
    volumes:
      - .:/app
      - ../../:/var/auth-kit-bundle
      - composer-cache:/root/.composer/cache
      - ./docker/php-dev.ini:/usr/local/etc/php/conf.d/99-php-dev.ini:ro
    env_file:
      - .env
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
      - SERVER_NAME=:80
      - DATABASE_URL=mysql://${MYSQL_USER:-demo}:${MYSQL_PASSWORD:-demo}@mysql:3306/${MYSQL_DATABASE:-auth_kit_demo}?serverVersion=8.0
    # REQ-DEMO-009: public DNS for Composer / Packagist (Docker/WSL resolver issues).
    dns:
      - 8.8.8.8
      - 8.8.4.4
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - auth-kit-demo-network

  mysql:
    image: mysql:8.0
    container_name: auth-kit-demo-symfony8-mysql
    env_file:
      - .env
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-auth_kit_demo}
      MYSQL_USER: ${MYSQL_USER:-demo}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-demo}
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - auth-kit-demo-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-root}"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  composer-cache:
    driver: local
  mysql-data:

networks:
  auth-kit-demo-network:
    driver: bridge
