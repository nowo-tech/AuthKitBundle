{#
    Override: templates/bundles/NowoAuthKitBundle/security/reset_request.html.twig
#}
{% extends layout_template %}

{% block title %}{{ 'reset.request.title'|trans({}, 'NowoAuthKitBundle') }}{% endblock %}

{% block body %}
{% form_theme reset_request_form 'form/auth_kit_theme.html.twig' %}
<div class="card shadow-sm border-0 auth-card">
    <div class="card-body p-4">
        <div class="auth-card__header">
            <h1 class="h4 mb-0 fw-semibold">{{ 'reset.request.heading'|trans({}, 'NowoAuthKitBundle') }}</h1>
        </div>
        <p class="text-muted small">{{ 'reset.request.help'|trans({}, 'NowoAuthKitBundle') }}</p>
        <p class="alert alert-secondary small py-2">{{ 'demo.usecase.reset_hint'|trans({}, 'demo') }}</p>

        {{ form_start(reset_request_form, {attr: {class: 'vstack gap-3'}}) }}
            {{ form_row(reset_request_form.identifier, {row_attr: {class: 'mb-0'}}) }}
            <div class="pt-1">
                <button type="submit" class="btn btn-primary w-100 btn-lg">{{ 'reset.request.submit'|trans({}, 'NowoAuthKitBundle') }}</button>
            </div>
        {{ form_end(reset_request_form) }}

        <div class="auth-card__footer text-center">
            <a href="{{ path(login_route, auth_kit_route_params()) }}" class="link-secondary text-decoration-none small">{{ 'reset.request.back_to_login'|trans({}, 'NowoAuthKitBundle') }}</a>
        </div>
    </div>
</div>
{% endblock %}
